{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          { "type": "command", "command": "uvx --isolated capt-hook pack attach \"${CLAUDE_PLUGIN_ROOT}/hooks\"" },
          { "type": "command", "command": "command -v cc-notes >/dev/null 2>&1 || curl -fsSL https://raw.githubusercontent.com/yasyf/cc-notes/main/scripts/install.sh | sh", "async": true }
        ]
      }
    ]
  }
}
